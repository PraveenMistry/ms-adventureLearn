services:
  - type: web
    name: ms-adventureLearn
    env: node
    plan: free
    rootDir: ms-adventureLearn
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
